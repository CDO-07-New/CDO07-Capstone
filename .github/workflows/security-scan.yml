name: security-scan

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop
      - main
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

env:
  PROJECT_DIR: xbrain-learners/capstone/tf-4/cdo-07

jobs:
  gitleaks:
    name: Gitleaks secret scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Scan working tree for secrets
        shell: bash
        run: |
          set -euo pipefail
          docker run --rm \
            -v "$PWD:/repo" \
            ghcr.io/gitleaks/gitleaks:v8.24.2 \
            dir /repo --redact --verbose

  trivy:
    name: Trivy filesystem scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Scan filesystem for vulnerabilities
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: '1'

  checkov:
    name: Checkov Terraform scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect Terraform files
        id: terraform
        shell: bash
        run: |
          set -euo pipefail
          if find "${PROJECT_DIR}/infra" -type f -name '*.tf' | grep -q .; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "::notice title=Terraform not present::No .tf files found under ${PROJECT_DIR}/infra; skipping Checkov."
          fi

      - name: Run Checkov
        if: steps.terraform.outputs.exists == 'true'
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: ${{ env.PROJECT_DIR }}/infra
          framework: terraform
          skip_check: CKV_AWS_356,CKV_AWS_108,CKV_AWS_109,CKV_AWS_111,CKV_AWS_18,CKV_AWS_144,CKV2_AWS_62,CKV_AWS_272,CKV_AWS_338,CKV2_AWS_5,CKV2_AWS_12,CKV_AWS_50,CKV_AWS_116,CKV_AWS_145,CKV_AWS_378,CKV_AWS_379,CKV_AWS_290,CKV_AWS_66,CKV_AWS_21,CKV_AWS_23,CKV_AWS_300,CKV_AWS_301,CKV_AWS_2,CKV_AWS_41,CKV_AWS_65,CKV_AWS_136,CKV_AWS_135,CKV_AWS_103,CKV_AWS_104,CKV_AWS_91,CKV_AWS_119,CKV_AWS_120,CKV_AWS_147,CKV_AWS_150,CKV_AWS_28,CKV_AWS_158,CKV_AWS_173,CKV_AWS_115,CKV_AWS_117,CKV_AWS_97,CKV_TF_1,CKV_AWS_355,CKV_AWS_337
          quiet: true
          soft_fail: false
